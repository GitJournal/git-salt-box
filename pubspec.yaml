name: gitjournal_crypt
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.14.4 <3.0.0"

dependencies:
  dart_git:
    git: https://github.com/GitJournal/dart_git.git

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
